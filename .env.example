# web = genius.com/api (default; works on a home IP, no token)
# official = api.genius.com (needed on Vercel and other cloud hosts)
# GENIUS_API=web

# Client access token from https://genius.com/api-clients
# Only required for GENIUS_API=official (or when this is set, official is the default)
# GENIUS_ACCESS_TOKEN=
